/**
 * In JS, variables and function declarations are hoisted to the top of their
 * scope. This means that you can use a variable or function before it is
 * declared.
 *
 * However, vaiable and function assignments are not hoisted.
 */
console.log(myName);
myName = "kiran";
